This project takes in data from either a file or through the command line and outputs the original value, the corresponding ASCII character, the corresponding decimal value, and whether it is even or odd.
